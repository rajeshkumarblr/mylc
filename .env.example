# Copy to .env and tweak as needed
# .env is gitignored; keep secrets (if any) out of version control

# Default language for ./run (cpp|go)
LC_LANG=cpp

# Optional defaults used by the runners (./run will export these when appropriate)
# LC_PROB_NUM=all     # uncomment to default to running all problems
# LC_CATEGORY=tree    # uncomment to default to a category
# LC_VERBOSE=1        # future per-test verbosity flag
